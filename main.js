/**
 * Devuelve Hello World, junto al valor que se le pase como parámetro
 *
 * @param   {string}  val   Un valor cualquiera
 *
 * @return  {string}        Devuelve un string
 */
function testFunction(val) {
    /**
     * Es un huevo
     * @type {string} 
     */
    var mihuevo = "huevo";

    return "Hello World, " + val + " " + mihuevo;
}